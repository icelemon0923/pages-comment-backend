# pages-comment-backend
一个简单的评论后端 API，使用 Flask 实现，可部署在 Vercel 上。

## API 接口
- `GET /api/comments` 获取所有评论
- `POST /api/comment` 提交新评论

## 部署
1. 将代码推送到 GitHub
2. 在 Vercel 中导入该仓库
3. 自动部署完成后，即可通过生成的 URL 访问 API
